# FIXME: the master key should be used to generate a more specific API key.
# But the full value of the API key is generated by Meilisearch on request, and
# I don't think Tutor supports storing the result of an 'init' function into a tutor config setting during init ?
MEILISEARCH_API_KEY: "{{ MEILISEARCH_MASTER_KEY }}"